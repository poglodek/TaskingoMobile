<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:TaskingoMobile.ViewModels"
             mc:Ignorable="d"
             x:Class="TaskingoMobile.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <Grid Style="{StaticResource Key=BackgroundGradient}">
            <Grid.RowDefinitions>
                <RowDefinition Height="4*" />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Label Text="Welcome to Taskingo" Style="{StaticResource Key=LabelWindow}"/>
            <Entry Style="{StaticResource Key=Entry}" Grid.Row="1" Placeholder="Email" />
            <Entry  Style="{StaticResource Key=Entry}" Grid.Row="2" Placeholder="Password" IsPassword="True" />
            <Entry  Style="{StaticResource Key=Entry}" Grid.Row="3" Placeholder="IP Address" />
            <Button  Grid.Row="4" VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
        </Grid>
        
    </ContentPage.Content>
</ContentPage>